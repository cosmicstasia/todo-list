# Todo App

Basic Todo app built using Bun, Hono, Drizzle-ORM, and React. Built as a project to learn TS fundamentals.

To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
bun run dev:frontend
```

open http://localhost:3000
